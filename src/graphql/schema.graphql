type Score {
    id: ID!
    name: String!
    score: Float!
    killedby: String!
    createdAt: String
    updatedAt: String
}

type Query {
    leaderboard: [Score!]!
    score(id: ID!): Score
}

type Mutation {
    score(name: String!, score: Float!, killedby: String!): Score
}